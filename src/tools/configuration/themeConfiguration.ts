import { createTheme } from '@mui/material';

export const theme = createTheme({
	palette: {
		primary: {
			main: 'primary',
			dark: '#2196F3',
			light: 'aaa'
		},
		secondary: {
			main: 'secondary',
			dark: '#2196F3',
			light: 'aaa'
		}
	}
});
